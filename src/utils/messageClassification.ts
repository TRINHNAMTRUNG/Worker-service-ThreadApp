import { messageValueType } from "../configs/kafka";

export const classifyMessage = <DataMessageType>(messages: messageValueType<DataMessageType>[]) => {

    messages.forEach(message => {

    });
}